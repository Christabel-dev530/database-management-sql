# database-management-sql
Manage and Improve the performance of MySQL Database with ManageEngine APM. Root Out Problem Queries &amp; Locate I/O Bottlenecks
# mysql.connector error
pip3 install mysql-connector-python

# command for upgrading the pip
python.exe -m pip install --upgrade pip

---------------------------------------------------------
# PROBLEM SCENARIO
-------------------------------------------------------------
1. In mysql server, create a database called customerdb. 
2. Inside the database, create a table called customers with the following fields/column: customer_name, email, product_name, product_price, product_id, purchse_date.
3. insert some records
4. Update records
5. Delete records